# Next.js 16 on Netlify — the official plugin handles the publish dir + runtime.
# Do NOT set `publish` manually; the @netlify/plugin-nextjs runtime manages it.
[build]
  command = "npm run build"

[[plugins]]
  package = "@netlify/plugin-nextjs"
